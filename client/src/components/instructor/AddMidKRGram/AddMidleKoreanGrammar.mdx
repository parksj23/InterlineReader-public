---
name: AddMidKRGramContainer
menu: Adding Middle Korean Grammar
---

import { Playground, Props } from 'docz'
import { Button } from '@material-ui/core'
import AddMidKRGramContainer from './AddMidKRGramContainer'

# Overview

The purpose fo the feature is to allow instructors to add Middle korean Grammar to the database.

This component fetches data from the redux store and passes it to its child components. The child components are:
- AddMidKrGram
- StatusMessage

# Props
<Props of={AddMidKRGramContainer}/>

# Basic Usage

# CSS Styling

